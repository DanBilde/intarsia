<article class="artworklist">
    <h1>Personlige miniature projekter</h1>
    <p> I galleriet nedenfor kan du se nogle af de projekter, som jeg har produceret på min fritid.Klik på billedet for at se oprindelige størrelse .</p>
    <div class="pixgrid">
        <% if(artwork.length > 0) { %>
            <% for(i=0; i <artwork.length;i++) { %>
                <img src="/images/artwork/<%= artwork[i] %>" alt="Artwork <%= i %>">
           <%  } %>
       <% } %>
    </div>
</article>